# TEA Encryption Algorithm

One of my first assembly projects, the TEA encryption algorithm takes a message from prog FLASH, encrypts it with the key in prog FLASH and EEPROM, and encrypts it into the final result in SRAM.

It was used for simulation only, in Microchip Studio.